// -------------------------------------------------------
// UTILITIES
// -------------------------------------------------------
function log(str) {
  console.log(str);
}

window.addEventListener("load", () => {
  const app = new MainApp(recipes);
});
