"use strict";

// -------------------------------------------------------
// UTILITIES
// -------------------------------------------------------
function log(str) {
  console.log(str);
}

window.addEventListener("load", function () {
  var app = new MainApp(recipes);
});